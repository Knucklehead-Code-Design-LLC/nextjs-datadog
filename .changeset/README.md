# Changesets

Add a changeset for every user-visible package change:

```bash
npm run changeset
```

Documentation, tests, and internal tooling changes do not require a changeset
unless they affect the published package or release behavior.
