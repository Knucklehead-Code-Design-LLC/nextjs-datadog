---
'nextjs-datadog': minor
---

Redact outbound HTTP URL paths by default, add an explicit safe-path opt-in, identify the package version on spans and logs, and document production-oriented Datadog queries and sampling behavior.
