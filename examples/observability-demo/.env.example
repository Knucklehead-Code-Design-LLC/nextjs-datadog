DD_ENV=development
DD_SERVICE=nextjs-datadog-demo
DD_VERSION=local

# Set this when the demo runs on a different port or calls a trusted local backend.
# DEMO_TRUSTED_BACKEND_ORIGIN=http://localhost:3000

# Optional: enable real browser RUM in addition to the local telemetry preview.
# NEXT_PUBLIC_DD_APPLICATION_ID=
# NEXT_PUBLIC_DD_CLIENT_TOKEN=
# NEXT_PUBLIC_DD_ENV=development
# NEXT_PUBLIC_DD_SERVICE=nextjs-datadog-demo
# NEXT_PUBLIC_DD_VERSION=local
